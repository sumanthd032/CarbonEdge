name: carbonedge
description: "CarbonEdge – Industrial ESG & Machine Monitoring Dashboard"
publish_to: 'none' # Remove if you ever want to publish

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  web_socket_channel: ^2.4.0
  google_generative_ai: ^0.2.0
  # UI & Charts
  cupertino_icons: ^1.0.8
  google_fonts: ^6.3.2
  fl_chart: ^0.68.0
  percent_indicator: ^4.2.3

  # Firebase
  firebase_auth: ^6.1.2
  google_sign_in: ^7.2.0
  cloud_firestore: ^6.1.0

  # File Export & Download (REQUIRED for Reports screen)
  path_provider: ^2.1.4
  pdf: ^3.11.1
  open_file: ^3.5.7
  excel: ^4.0.6
  csv: ^6.0.0
  printing: ^5.13.4
  intl: ^0.19.0
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Assets
  assets:
    - assets/
    # Add your logo, icons, images here later
    # - assets/images/logo.png

  # Optional: Custom fonts (uncomment and add when ready)
  # fonts:
  #   - family: Roboto
  #     fonts:
  #       - asset: fonts/Roboto-Regular.ttf
  #       - asset: fonts/Roboto-Bold.ttf
  #         weight: 700